import { Meta } from "@storybook/addon-docs/blocks";

<Meta title="Overview" />

# Magnitude compare

**Magnitude compare** is a calculator for two earthquake magnitudes. It is not a wave-equation solver.

## What it computes

- **Amplitude** `A ∝ 10^M` — original Richter local magnitude ML, same station and instrument. Each +1 is **10×** amplitude.
- **Radiated energy** `log10(Es) = 1.5M + 4.4` (Kanamori 1977). Each +1 is **~31.6×** energy.
- **Seismic moment** via Hanks & Kanamori, treating the input as Mw.
- **TNT equivalent** as an order-of-magnitude metaphor only.

## Storybook map

| Group                      | Stories                            |
| -------------------------- | ---------------------------------- |
| **Magnitude/Compare**      | Full calculator                    |
| **Magnitude/MagField**     | Magnitude text + slider            |
| **Magnitude/LogBars**      | Shared log scale amp vs energy     |
| **Magnitude/GrowthCurves** | Two slopes vs ΔM                   |
| **Magnitude/EnergyTiles**  | How many A events match one B      |
| **Seismograph/Stylus**     | Dual-channel VFD traces + needles  |
| **UI/**                    | StatCard, ChipButton, SectionLabel |

## Visuals

- Dual Ricker traces at the same station: the larger event fills the plot; the smaller is scaled by `10^(−ΔM)`.
- Shared log-axis bars (1× at center) so energy visibly outruns amplitude.
- Growth curves: amplitude slope 1 vs energy slope 1.5 on log(B/A) vs ΔM.

## Limitations

ML saturates above ~6.5 (energy and moment treat the number as Mw). Amplitude ratios assume the same distance and instrument. MMI is not magnitude. Es is radiated seismic energy, not total strain energy. TNT is not a blast yield.
